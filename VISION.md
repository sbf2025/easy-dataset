# Easy Dataset — 项目愿景与需求整合文档

> 本文档将两套核心需求整合为统一的项目愿景，用于校准 Easy Dataset 的开发边界与改进方向。

---

## 一、项目定位与核心价值

Easy Dataset 是一个**面向领域知识的 LLM 微调数据集智能生成平台**。它的核心价值在于：将非结构化的领域文档（Markdown、PDF 等）转化为高质量的结构化训练数据（问答对、指令集），帮助各行业团队快速构建专属的领域智能体。

在医疗健康领域，特别是**妇幼保健**这一高专业度、高人文关怀要求的场景中，Easy Dataset 扮演着至关重要的"知识基础设施"角色：
- 为早孕关爱门诊智能体提供知识库问答对训练数据
- 为区域妇幼健康整合型服务系统（RIMC）提供 LLM 能力底座
- 将妇幼保健临床知识、诊疗规范、随访方案系统化转化为可训练数据集

---

## 二、双轨需求整合

### 2.1 轨道一：区域妇幼健康整合型服务系统（RIMC）

#### 背景
当前医疗系统存在"信息孤岛"问题：同一机构内各专科医生组之间缺乏协同，跨机构（市-区县-乡镇）之间的转诊数据不互通，导致孕产妇在整个生命周期中面临"服务断链"风险。

#### 目标：二维妇幼服务网络

| 维度 | 描述 | 典型场景 |
|------|------|----------|
| 横向（院内全流程） | 同院内多专科医生组间的患者无缝流转 | 孕产保健组 → 分娩组 → 产后康复组 |
| 纵向（跨机构连续） | 市级龙头医院牵头，下至区县、乡镇的分级诊疗 | 高危孕产妇从乡镇卫生院转至市妇幼保健院 MDT 组 |

#### 核心技术设计（Go 后端）

**数据模型（Phase 1）**

```
data/
├── config/
│   ├── institutions.json   # 机构档案（市/区县/乡镇三级）
│   └── users.json          # 人员档案（医生/护士/管理员）
├── matching/
│   ├── doctor_groups.json  # 医生组（如"孕产保健组"、"高危MDT组"）
│   ├── patient_groups.json # 患者群组（如"孕37周+待产群"）
│   └── group_relations.json # 供需匹配关系
└── records/
    ├── patients.json        # 患者档案（含生命周期阶段）
    └── service_transfers.json # 服务流转单（核心业务凭证）
```

**核心 Go Struct（ServiceTransfer）**

```go
// ServiceTransfer 服务流转单 - 统一处理院内流转与跨院转诊
type ServiceTransfer struct {
    ID                string         `json:"id"`
    PatientID         string         `json:"patientId"`
    FromInstitutionID string         `json:"fromInstitutionId"`
    FromDoctorGroupID string         `json:"fromDoctorGroupId"`
    ToInstitutionID   string         `json:"toInstitutionId"`
    ToDoctorGroupID   string         `json:"toDoctorGroupId"`
    TransferType      TransferType   `json:"transferType"` // Internal | External
    Status            TransferStatus `json:"status"`
    Reason            string         `json:"reason"`
    CreatedAt         time.Time      `json:"createdAt"`
}

type TransferType   string
const (
    Internal TransferType = "Internal" // 院内专科流转
    External TransferType = "External" // 跨院转诊
)
```

**后端架构约束**

| 约束项 | 规范 |
|--------|------|
| 语言栈 | 纯 Go（标准库 + Gin） |
| 存储方式 | 本地 JSON 文件，禁止使用任何外部数据库 |
| 数据访问层 | 必须定义 `interface`（`TransferRepository`、`PatientRepository`），确保未来平滑迁移至真实 DB |
| 并发安全 | 所有内存数据操作使用 `sync.RWMutex` 保护 |
| 持久化策略 | 内存更新后，通过 Goroutine 异步原子写入（先写 `.tmp`，再 `os.Rename`） |

**全生命周期流转引擎（Phase 2）**

```go
// ProcessPatientVitals 处理患者体征/孕周更新，自动触发群组变更和流转
func ProcessPatientVitals(patient *Patient, vitals map[string]interface{}) (*ServiceTransfer, error)
```

触发逻辑：
- 孕周变更 → 评估患者群组归属 → 若需变更则自动生成 `ServiceTransfer`
- 孕期群 → 待产群：生成**院内流转单**（Internal），目标：分娩中心医生组
- 高危预警触发：生成**跨院转诊单**（External），目标：上级机构高危 MDT 组

**权限过滤中间件（Phase 3）**

```
请求头 UserID
    ↓
解析所属 Institution + DoctorGroup
    ↓
数据作用域过滤：
  - 普通医生：只能看自己 DoctorGroup 负责的患者 + 待接收的 ServiceTransfer
  - 机构管理员：可查看院内全部跨组流转（横向）+ 与下属机构的跨院转诊（纵向）
```

---

### 2.2 轨道二：早孕关爱门诊智能体（ECCA）

#### 背景
早孕期（0-12 周）是孕产妇风险识别最重要但也最容易被忽视的阶段。现有医疗资源有限，无法为每位孕产妇提供全天候的个性化关怀和专业指导，导致：
- 孕产妇信息获取渠道碎片化，真假难辨
- 风险症状（出血、腹痛等）识别不及时
- 随访不规范，复诊提醒缺失

#### 核心能力

| 能力模块 | 描述 | 实现状态 |
|----------|------|----------|
| 智能问答 | 基于妇幼保健知识库（RAG）回答早孕常见问题 | 规划中 |
| 风险初筛 | 通过问诊逻辑识别出血、腹痛等高风险症状，提示及时就诊 | 规划中 |
| 随访提醒 | 根据孕周自动推送复诊时间、检查项目、用药依从性提醒 | 规划中 |
| 人文关怀 | 产前焦虑、心理健康支持，使用温暖而专业的语言风格 | 规划中 |
| 医生工作台 | 医生后台查看患者咨询记录，辅助临床决策 | 规划中 |

> ⚠️ 重要声明：智能体提供的所有信息仅为**辅助决策、提示和参考**，不具备法律上的诊断或治疗效力。所有医疗决策必须由有执照的医疗专业人员作出。

#### 技术架构

```
早孕关爱门诊智能体（ECCA）架构

┌─────────────────────────────────────────┐
│              前端层                      │
│  Web / 微信小程序 / 移动 APP             │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│              API 网关 / 后端服务         │
│  (Next.js API Routes / Go Gin)          │
└───┬─────────────────────────────────────┘
    │
    ├─── LLM 调用层 ──────────────────────┐
    │    OpenAI / 本地模型 / RAG 检索      │
    │    妇幼保健知识库（向量数据库）        │
    └─────────────────────────────────────┘
    │
    ├─── 业务逻辑层 ──────────────────────┐
    │    早孕问诊流程引擎                   │
    │    风险评估规则引擎                   │
    │    随访计划管理器                     │
    └─────────────────────────────────────┘
    │
    └─── 数据存储层 ──────────────────────┐
         JSON 文件（开发/轻量部署）          │
         PostgreSQL / MySQL（生产部署）      │
         对象存储（文档/图片）               │
         日志与审计系统                      │
         └────────────────────────────────┘
```

#### Easy Dataset 的角色

Easy Dataset 是构建 ECCA 知识能力的核心工具：

1. **知识库构建**：将妇幼保健临床指南、早孕期护理手册等 Markdown 文档导入 Easy Dataset，批量生成问答对
2. **模型微调**：将生成的问答数据集（Alpaca/ShareGPT 格式）用于微调领域专属 LLM
3. **RAG 语料**：将分割后的文本片段作为向量检索的基础语料库
4. **持续迭代**：随着临床实践积累，不断补充新的知识文档，通过 Easy Dataset 更新训练数据

---

## 三、整合后的项目发展路线图

### 3.1 短期目标（当前 - 2026 Q2）

Easy Dataset 作为**数据基础设施工具**，重点提升：

- [ ] **医疗文档格式支持**：增加对医疗常用格式（PDF、Word 临床指南）的解析能力
- [ ] **领域感知的问题生成**：针对医疗场景优化提示词，生成更符合临床逻辑的问答对
- [ ] **数据质量评估模块**：对生成的问答对进行自动质量评分，过滤低质量数据
- [ ] **批量处理优化**：支持大规模文档批量处理，满足医院知识库建设需求
- [ ] **安全与隐私增强**：确保患者信息不进入训练数据，满足医疗数据合规要求

### 3.2 中期目标（2026 Q3 - 2026 Q4）

**RIMC 系统集成**（区域妇幼健康整合型服务系统）：

- [ ] Go 后端核心：机构管理、医生组、患者档案 CRUD API
- [ ] 服务流转引擎：全生命周期流转逻辑（院内 Internal + 跨院 External）
- [ ] 权限系统：基于医生组的数据作用域控制中间件
- [ ] 前端工作台：医生多身份工作台 + 流转任务看板
- [ ] 管理后台：全周期流转网络可视化配置界面

**ECCA 早孕关爱智能体**：

- [ ] 知识库基础建设（使用 Easy Dataset 生成训练数据）
- [ ] RAG 检索模块：向量化妇幼保健知识，实现语义检索
- [ ] 基础问答能力：早孕期常见问题智能回答
- [ ] 风险初筛逻辑：结构化问诊 + 症状风险分级

### 3.3 长期目标（2027 及以后）

- [ ] 全孕产期覆盖：从早孕扩展至孕中晚期、产后、新生儿保健
- [ ] 多中心验证：从单院试点走向多机构部署，积累循证数据
- [ ] 医疗合规认证：申请相关医疗软件认证，完善数据隐私保护
- [ ] 知识图谱增强：构建妇幼保健领域知识图谱，增强 AI 推理能力
- [ ] 行业标准化：形成可复用的妇幼健康 AI 解决方案，推动行业标准建设

---

## 四、技术约束与开发规范

### 4.1 Go 后端技术规范（RIMC 系统）

```
严格遵守以下约束（不可妥协）：
✅ 纯 Go 语言栈：后端 100% Go，禁止混入 Python/Node 等其他语言
✅ 无外部数据库：禁止使用 MySQL/Redis/PostgreSQL 等（初始版本）
✅ 文件系统持久化：data/*.json 存储所有业务数据
✅ 面向接口编程：Repository 层必须通过 interface 定义
✅ 并发安全：所有共享状态使用 sync.RWMutex
✅ 原子写入：文件写入采用 tmp + rename 原子操作
✅ Effective Go 规范：命名、错误处理、包设计严格遵循官方规范
```

### 4.2 前端技术规范（沿用 Easy Dataset 技术栈）

- 框架：Next.js 14 (App Router) + React 18
- UI：Material-UI (MUI) 5.x
- 国际化：i18next（中英双语）
- 状态管理：React Context / SWR

### 4.3 数据安全规范

| 规范项 | 要求 |
|--------|------|
| 患者隐私 | 训练数据生成时必须脱敏，禁止包含真实患者 ID、姓名、联系方式 |
| 数据分级 | 明确区分：公开知识（可训练）、临床记录（需脱敏）、个人隐私（禁止使用） |
| 访问控制 | 基于角色（医生/管理员）的最小权限原则 |
| 审计日志 | 所有数据访问操作记录可追溯的操作日志 |

---

## 五、参与与贡献

本项目欢迎来自不同背景的贡献者：

| 角色 | 可贡献方向 |
|------|-----------|
| **AI/ML 工程师** | LLM 集成优化、RAG 模块、模型微调流水线 |
| **Go 后端开发者** | RIMC 系统 API 开发、流转引擎、中间件 |
| **前端开发者** | 医生工作台 UI、流转任务看板、可视化组件 |
| **妇幼保健医生** | 知识库内容审核、临床逻辑验证、风险规则设计 |
| **产品/运营** | 用户体验优化、需求收集、文档完善 |
| **医疗合规专家** | 数据安全、合规认证、隐私保护方案 |

最快的参与路径：
1. 先从文档修订和问题反馈开始（issues）
2. 选择 `good first issue` 标签的任务
3. 为妇幼保健知识库贡献高质量文档素材

---

## 六、免责声明

本项目中所有涉及医疗健康的功能模块均为**辅助决策工具**，不能替代专业医疗建议。所有医疗相关内容仅供参考，实际诊断、治疗决策必须由具有执业资格的医疗专业人员负责。
